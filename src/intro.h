#ifndef _INTRO_H_
#define _INTRO_H_

// The club's logo (Retro Computer Dresden e.V.) at power-on, taken over 1:1 from the Wanderburg
// project: it sweeps in from left to right behind a white shine. Blocks until it has been shown
// (or a button skips it), then clears the screen and restores the text palette.
void intro_run(void);

#endif
