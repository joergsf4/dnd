#ifndef _TITLE_H_
#define _TITLE_H_

// The title screen (tools/make_title.py): the Nautiloid before the burning sky, "START DRÜCKEN".
// Blocks until START or A, then leaves both planes cleared.
void title_run(void);

#endif
